{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit profile</h1>

        <a href="{{ path('user_password') }}">
            <button class="btn btn-pink">Edit password</button>
        </a>

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-pink">Save</button>
        {{ form_end(form) }}

    </div>
{% endblock %}